# Commits listed here are mechanical, non-semantic sweeps (ruff autofix
# passes) — `git blame` skips them so authorship attribution for the
# surrounding lines stays meaningful through the RL-8 API-surface audit.
#
# Configure locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# (plans/017-ci-green-workflows-and-lint-type-gates.md, Step 9). The plan's
# Step 5/6/7 called for one commit per rule family; this landed as a single
# combined ruff-sweep + mypy-suppression commit instead (see that commit's
# own message for why) — it is not purely mechanical (it also carries
# hand-written `# type: ignore[...]`/`# noqa: ...` suppressions and a
# handful of real bug fixes), so treat `git blame` output on lines it
# touches with that caveat in mind.
f03e6133b9bc62c77a2052e5ee0032952dd79e49

# chore(format): adopt ruff format, replacing black (618 files reformatted).
# black formatted to its 88-column default while [tool.ruff] pins
# line-length = 100. Purely mechanical apart from one relocated
# `# type: ignore[safe-super]` comment in observability/repository_mixin.py.
db35156ed88b92f8e17bea4b58359bc180cf260a
