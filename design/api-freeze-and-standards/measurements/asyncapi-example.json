{
  "asyncapi": "3.1.0",
  "info": {
    "title": "varco example — full-stack post API",
    "version": "0.1.0",
    "description": "Generated by `varco export-asyncapi` from live, registered EventConsumer instances. A consumer that is never wired to a bus is deliberately absent.\n\nBinding coverage: **Kafka** bindings are emitted (channel `topic`, operation `groupId`) because the Kafka binding specification defines real fields. **NATS** carries exactly one operation field (`queue`), so its binding is emitted only when a queue group is configured — a stanza carrying nothing but `bindingVersion` is noise. **Redis** has no binding fields at any level, so no Redis binding block is emitted at all; a channel's `address` already carries everything Redis has."
  },
  "channels": {
    "posts": {
      "address": "posts",
      "messages": {
        "PostCreatedEvent": {
          "name": "PostCreatedEvent",
          "title": "PostCreatedEvent",
          "payload": {
            "description": "Emitted after a ``Post`` is successfully created and committed.\n\nConsumers use this event to trigger side-effects: sending notifications,\nupdating a search index, incrementing counters, etc.\n\nArgs:\n    post_id:   UUID of the newly created post.\n    author_id: UUID of the user who authored the post.\n\nEdge cases:\n    - Published AFTER the UoW commits — the post IS durable when this\n      event fires.  Consumers may safely fetch the post by ``post_id``.\n    - If the bus is unavailable, the event is lost (no outbox pattern here).\n      For guaranteed delivery, persist in the same transaction via\n      ``OutboxRepository``.",
            "properties": {
              "event_id": {
                "format": "uuid",
                "title": "Event Id",
                "type": "string"
              },
              "timestamp": {
                "format": "date-time",
                "title": "Timestamp",
                "type": "string"
              },
              "post_id": {
                "format": "uuid",
                "title": "Post Id",
                "type": "string"
              },
              "author_id": {
                "anyOf": [
                  {
                    "format": "uuid",
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Author Id"
              }
            },
            "required": [
              "post_id",
              "author_id"
            ],
            "title": "PostCreatedEvent",
            "type": "object"
          }
        },
        "PostDeletedEvent": {
          "name": "PostDeletedEvent",
          "title": "PostDeletedEvent",
          "payload": {
            "description": "Emitted after a ``Post`` is successfully deleted and committed.\n\nConsumers use this event to clean up related data: remove from search\nindex, revoke cached entries in other services, audit logging, etc.\n\nArgs:\n    post_id: UUID of the deleted post.  The post is no longer retrievable\n             from the repository when this event fires.\n\nEdge cases:\n    - The post is GONE when this fires — consumers must not call\n      ``service.get(post_id)`` expecting to find it.\n    - Use ``author_id`` if needed — look it up BEFORE calling ``delete()``\n      or extend this event to carry it.",
            "properties": {
              "event_id": {
                "format": "uuid",
                "title": "Event Id",
                "type": "string"
              },
              "timestamp": {
                "format": "date-time",
                "title": "Timestamp",
                "type": "string"
              },
              "post_id": {
                "format": "uuid",
                "title": "Post Id",
                "type": "string"
              }
            },
            "required": [
              "post_id"
            ],
            "title": "PostDeletedEvent",
            "type": "object"
          }
        }
      }
    }
  },
  "operations": {
    "posts.PostEventConsumer.on_post_created": {
      "action": "receive",
      "channel": {
        "$ref": "#/channels/posts"
      },
      "messages": [
        {
          "$ref": "#/channels/posts/messages/PostCreatedEvent"
        }
      ]
    },
    "posts.PostEventConsumer.on_post_deleted": {
      "action": "receive",
      "channel": {
        "$ref": "#/channels/posts"
      },
      "messages": [
        {
          "$ref": "#/channels/posts/messages/PostDeletedEvent"
        }
      ]
    }
  }
}
